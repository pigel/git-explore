git cd:/      */path/*

git config --global user.name"name"
git config --global user.email"user@name" */ config git/*

git config --list */ view config/*
git init */makes git directory/*
git status */show directory content and branches/*
git add .  */adding all files in directory/*
git commit -m "comment" */commit files (makes shot)/*
git branch new-branch */creates new branch/*
git checkout new-branch */switch branches/*

git clone https://github.com/pigel/new.git */clone from github repo/*
git remote add origin https://github.com/pigel/new.git */set remote from github repo/*

git push -u origin master */add files from git to github by origin remote/*
git push --set-upstream origin lomaster */streaming lomaster to github by origin remote/*

git pull */fetching files from github/*







